
wb = xlsx_package.workbook


wb.add_worksheet(name: "PolicyCategories") do |sheet|
  # Create the header row
  sheet.add_row ["name"]
  # Create entries for each item
  @policy_categories.each do |policy_category|
    sheet.add_row [policy_category&.name]
  end
end
